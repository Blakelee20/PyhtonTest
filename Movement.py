#imports
import time
import random
#functions
def location_compare(og):
	#record gps location
	return location_compare

#global variables
og=(9)
nl=random.randrange(0,9)
#main code
while(1):
	time.sleep(5)
	if nl!=og:
		#record new location
		#send alert
		print "Not Equal"
	else:
		print "Equal"
		continue
#wait statement with timer of about 15 minutes
#after timer expires record location
#compare the recorded location with the original location
#if recorded location is different by 5???
#then record that location
#then send an alert